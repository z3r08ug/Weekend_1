<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.uublabs.weekend_1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RosterActivity"
            android:label="@string/title_activity_roster"
            android:launchMode="singleInstance"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="dev.uublabs.weekend_1.HomeActivity" />
        </activity>
        <activity
            android:name=".ScheduleActivity"
            android:label="@string/title_activity_schedule"
            android:launchMode="singleInstance"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="dev.uublabs.weekend_1.HomeActivity" />
        </activity>
        <activity
            android:name=".StandingsActivity"
            android:label="@string/title_activity_standings"
            android:launchMode="singleInstance"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="dev.uublabs.weekend_1.HomeActivity" />
        </activity>
        <activity
            android:name=".TeamHome"
            android:label="@string/title_activity_team_home"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="dev.uublabs.weekend_1.HomeActivity" />
        </activity>
        <activity
            android:name=".TicketsActivity"
            android:label="@string/title_activity_tickets"
            android:launchMode="singleInstance"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="dev.uublabs.weekend_1.HomeActivity" />
        </activity>
        <activity
            android:name=".GronkActivity"
            android:label="@string/title_activity_gronk"
            android:launchMode="singleInstance"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="dev.uublabs.weekend_1.HomeActivity" />
        </activity>
    </application>

</manifest>